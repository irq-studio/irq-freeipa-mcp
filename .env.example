# FreeIPA MCP Server Environment Variables
# Note: These override values in config.yaml

# FreeIPA Server Configuration
FREEIPA_SERVER=ipa.example.com
FREEIPA_USERNAME=automation@EXAMPLE.COM
FREEIPA_PASSWORD=your-password-here

# SSH Configuration for SSSD Cache Management
SSH_USERNAME=automation
SSH_PASSWORD=your-ssh-password-here

# SSSD Domain (overrides config.yaml)
# SSSD_DOMAIN=example.com

# Optional: Enable debug logging
# DEBUG=true

# Note: For full configuration options including timeouts and SSSD settings,
# copy config.yaml.example to config.yaml and customize it.
